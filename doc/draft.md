# 草稿

一些todo:
1. 当前“事件类型说明” legend_widget 的 UI 不够紧凑，中间有很大的间隔，需要调整  
2. legend_widget 不能随着下滑而隐藏，需要支持这个功能。在用户上划时，可以只显示一个这个widget的缩略版  
3. “筛选用户” widget 不能随着下滑而隐藏，需要支持这个功能。在用户上划时，可以只显示一个这个widget的缩略版  
4. “筛选用户” widget 和 “事件类型说明” widget 可以放在一行之内，这样可以同时控制用户和事件类型的显示，此外还可以节省空间。  
   4.1 在用户下滑时，可以将这两个组件同时隐藏起来，只显示一个缩略版的 widget  
   4.2 在用户上划时，可以将这两个组件同时显示出来  
5. 标题Github Follow Contributions 样式不太令人满意，文字可以改成 "用户活动追踪"？或者其他更好的标题。此外如果可以支持不同语言下显示不同的标题就好了。例如中文下可以显示 "GitHub 用户活动追踪"，英文下可以显示 "Github User Activity Tracking"  
6. 在手机端很容易出现用户头像无法加载的情况。如果第一次没有加载成功，可以间隔性的重试加载，直到加载成功为止。
